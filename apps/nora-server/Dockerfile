# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @nora/nora-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nora-nora-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nora-nora-server && \
          adduser --system -G nora-nora-server nora-nora-server

COPY apps/nora-server/dist nora-nora-server/
COPY apps/nora-server/package.json nora-nora-server/
RUN chown -R nora-nora-server:nora-nora-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nora-nora-server --omit=dev -f install

CMD [ "node", "nora-nora-server" ]
